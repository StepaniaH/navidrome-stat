services:
  navidrome-stat:
    build: .
    container_name: navidrome-stat
    ports:
      - "39421:39421"
    volumes:
      - ./navidrome_stats.db:/app/navidrome_stats.db
    env_file:
      - .env
    restart: unless-stopped
