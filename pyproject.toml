[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [
    "node_modules",
    "src/static/vendor",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.coverage.run]
branch = true
source = ["src"]

[tool.coverage.report]
show_missing = true
skip_covered = true
